<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:settings="clr-namespace:DCCPanelController.View.Settings"
             xmlns:wiThrottle="clr-namespace:DCCPanelController.View.Settings.WiThrottle"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:discovery="clr-namespace:DCCPanelController.Clients.Discovery"
             x:DataType="wiThrottle:WiThrottleSettingsViewModel"
             x:Class="DCCPanelController.View.Settings.WiThrottle.WiThrottleSettingsView">

    <ContentView.Resources>
        <Style x:Key="SelectableItemStyle" TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="Transparent"/>
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="CornflowerBlue"/>
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentView.Resources>


    <VerticalStackLayout Margin="10,0,10,0">
        <VerticalStackLayout Margin="10,0,10,0">
            <Label Style="{StaticResource MediumLabel}" HorizontalOptions="Start" VerticalOptions="Start"
                   Text="Device Name (unique name for this Device)"/>
            <HorizontalStackLayout>
                <Border Padding="0" WidthRequest="250" HeightRequest="35" Style="{StaticResource EntryBorderStyle}">
                    <Entry Margin="10,0,10,0" HorizontalOptions="Fill" Text="{Binding WiThrottleSettings.Name}"/>
                </Border>
                <ImageButton Margin="2,0,0,0" Aspect="Center"
                             Source="target.png" WidthRequest="15" HeightRequest="15"
                             Command="{Binding DefaultDeviceNameClickedCommand }">
                    <ImageButton.Behaviors>
                        <toolkit:IconTintColorBehavior TintColor="{StaticResource Primary}"/>
                    </ImageButton.Behaviors>

                </ImageButton>
            </HorizontalStackLayout>
        </VerticalStackLayout>

        <HorizontalStackLayout Margin="10,10,0,0">
            <Label Text="Auto Set Connection Details?" HorizontalOptions="Start" Style="{StaticResource MediumLabel}"
                   VerticalOptions="Center" WidthRequest="260"/>
            <Switch IsToggled="{Binding WiThrottleSettings.SetAutomatically}"
                    OnColor="{StaticResource Primary}"
                    ThumbColor="{StaticResource Gray200}"/>
        </HorizontalStackLayout>

        <VerticalStackLayout Margin="10,10,0,0" IsEnabled="{Binding WiThrottleSettings.SetManually}">
            <Label Style="{StaticResource MediumLabel}"
                   HorizontalOptions="Start" VerticalOptions="Start"
                   Text="WiThrottle Address and Port"/>

            <HorizontalStackLayout HorizontalOptions="Start" Margin="0,-25,0,0">
                <Border WidthRequest="45" Padding="0" HeightRequest="35" Style="{StaticResource EntryBorderStyle}">
                    <Entry Focused="OnEntryFocused"
                           HorizontalOptions="Fill"
                           HorizontalTextAlignment="Center" Text="{Binding IpAddress1, Mode=TwoWay}">
                        <Entry.Behaviors>
                            <settings:EntryValidationBehavior/>
                        </Entry.Behaviors>
                    </Entry>
                </Border>
                <Label FontSize="48" Margin="2,2,2,30" Text="." TextColor="{StaticResource Primary}"
                       VerticalOptions="Center"/>
                <Border WidthRequest="45" Padding="0" HeightRequest="35" Style="{StaticResource EntryBorderStyle}">
                    <Entry
                        Focused="OnEntryFocused"
                        HorizontalOptions="Fill"
                        HorizontalTextAlignment="Center" Text="{Binding IpAddress2, Mode=TwoWay}">
                        <Entry.Behaviors>
                            <settings:EntryValidationBehavior/>
                        </Entry.Behaviors>
                    </Entry>
                </Border>
                <Label FontSize="48" Margin="2,2,2,30" Text="." TextColor="{StaticResource Primary}"
                       VerticalOptions="Center"/>
                <Border WidthRequest="45" Padding="0" HeightRequest="35" Style="{StaticResource EntryBorderStyle}">
                    <Entry
                        Focused="OnEntryFocused"
                        HorizontalOptions="Fill"
                        HorizontalTextAlignment="Center" Text="{Binding IpAddress3, Mode=TwoWay}">
                        <Entry.Behaviors>
                            <settings:EntryValidationBehavior/>
                        </Entry.Behaviors>
                    </Entry>
                </Border>
                <Label FontSize="48" Margin="2,2,2,30" Text="." TextColor="{StaticResource Primary}"
                       VerticalOptions="Center"/>
                <Border WidthRequest="45" Padding="0" HeightRequest="35" Style="{StaticResource EntryBorderStyle}">
                    <Entry
                        Focused="OnEntryFocused"
                        HorizontalOptions="Fill"
                        HorizontalTextAlignment="Center" Text="{Binding IpAddress4, Mode=TwoWay}">
                        <Entry.Behaviors>
                            <settings:EntryValidationBehavior/>
                        </Entry.Behaviors>
                    </Entry>
                </Border>
                <Label FontSize="48" HorizontalOptions="Center" Margin="2,2,2,10" Text=":"
                       TextColor="{StaticResource Primary}" VerticalOptions="Center"/>

                <Border WidthRequest="90" Padding="0" HeightRequest="35" Style="{StaticResource EntryBorderStyle}">
                    <Entry
                        Focused="OnEntryFocused"
                        HorizontalTextAlignment="Center"
                        HorizontalOptions="Fill"
                        Text="{Binding WiThrottleSettings.Port, Mode=TwoWay, FallbackValue='0'}"/>
                </Border>
            </HorizontalStackLayout>
        </VerticalStackLayout>

        <Button
            Command="{Binding ConnectClickedCommand}"
            HorizontalOptions="Start"
            IsEnabled="{Binding IsNotBusy}"
            Margin="20,0,20,20"
            Style="{StaticResource ButtonOutline}"
            Text="{Binding ConnectLabel}"
            WidthRequest="180"/>

        <BoxView
            BackgroundColor="{StaticResource Primary}"
            HeightRequest="2"
            HorizontalOptions="FillAndExpand"/>

        <Grid RowDefinitions="auto,2,*">
            <StackLayout Grid.Row="0">
                <Grid ColumnDefinitions="*,150">
                    <Label
                        Grid.Column="0"
                        HorizontalOptions="Start"
                        Style="{StaticResource MediumLabel}"
                        TextColor="{StaticResource Primary}"
                        VerticalOptions="Center">
                        Available Servers :
                    </Label>
                    <Button
                        BackgroundColor="Transparent"
                        Command="{Binding RefreshServersClickedCommand}"
                        Grid.Column="1"
                        HorizontalOptions="End"
                        ImageSource="refresh_cw.png"
                        Text="Refresh"
                        TextColor="{StaticResource Primary}"
                        VerticalOptions="Center"/>
                </Grid>
                <BoxView
                    BackgroundColor="{StaticResource Primary}"
                    HeightRequest="1"
                    HorizontalOptions="FillAndExpand"/>
                <RefreshView VerticalOptions="Start" MinimumHeightRequest="100"
                             Command="{Binding RefreshServersClickedCommand}"
                             IsRefreshing="{Binding IsRefreshing}">
                    <Grid>
                        <CollectionView ItemsSource="{Binding Servers}" SelectionMode="Single"
                                        SelectionChanged="SelectableItemsView_OnSelectionChanged">
                            <CollectionView.EmptyView>
                                <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
                                    <Label IsVisible="{Binding IsNotBusy}" HorizontalOptions="Center"
                                           Text="Use Refresh to search for available servers"/>
                                    <Label IsVisible="{Binding IsBusy}" HorizontalOptions="Center"
                                           Text="Searching for available servers"/>
                                </StackLayout>
                            </CollectionView.EmptyView>

                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="discovery:DiscoveredService">
                                    <Grid Style="{StaticResource SelectableItemStyle}">
                                        <StackLayout>
                                            <Grid ColumnDefinitions="48,*">
                                                <Label
                                                    Grid.Column="0"
                                                    HorizontalOptions="Start"
                                                    Margin="5"
                                                    Text="◉"
                                                    VerticalOptions="Center"
                                                    x:Name="SelectedServerTick"/>
                                                <Label
                                                    Grid.Column="1"
                                                    HorizontalOptions="Start"
                                                    LineBreakMode="NoWrap"
                                                    Margin="0,5,0,5"
                                                    Style="{StaticResource MediumLabel}"
                                                    Text="{Binding FriendlyName}"
                                                    VerticalOptions="Center"
                                                    x:Name="SelectedServerText"/>
                                            </Grid>
                                            <BoxView
                                                BackgroundColor="{StaticResource Primary}"
                                                HeightRequest="0.5"
                                                HorizontalOptions="FillAndExpand"/>
                                        </StackLayout>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </Grid>
                </RefreshView>
            </StackLayout>
        </Grid>
    </VerticalStackLayout>

</ContentView>