<?xml version="1.0" encoding="utf-8"?>

<ContentPage
    IconImageSource="help_circle.png"
    Title="Help"
    iOsSpecific:Page.UseSafeArea="True"
    x:Class="DCCPanelController.View.HelpPage"
    x:DataType="view:HelpPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:iOsSpecific="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:DCCPanelController.View">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Home"    Clicked="OnHome"    Priority="0" Order="Primary"                                            IconImageSource="home_active" />
        <ToolbarItem Text="Back"    Clicked="OnBack"    Priority="0" Order="Primary" IsEnabled="{Binding BackButtonEnabled}"    IconImageSource="{Binding BackButtonEnabled, Converter={StaticResource BoolToIcon}, ConverterParameter=arrow_left}"/>
        <ToolbarItem Text="Forward" Clicked="OnForward" Priority="1" Order="Primary" IsEnabled="{Binding ForwardButtonEnabled}" IconImageSource="{Binding ForwardButtonEnabled, Converter={StaticResource BoolToIcon}, ConverterParameter=arrow_right}"/>
    </ContentPage.ToolbarItems>
    
    <Grid RowDefinitions="*" Padding="10">
        <view:HelpViewer x:Name="Viewer" Grid.Row="1" HorizontalOptions="Fill" VerticalOptions="Fill"/>
    </Grid>

</ContentPage>


