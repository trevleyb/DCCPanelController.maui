<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:DCCPanelController.ViewModel"
             xmlns:view="clr-namespace:DCCPanelController.View"
             Title="{Binding SelectedPanel.Name}"
             x:DataType="viewModel:OperateViewModel"
             x:Class="DCCPanelController.View.OperatePage">
    
    <ContentPage.BindingContext>
        <viewModel:OperateViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid ColumnDefinitions="*" RowDefinitions="*,Auto" Margin="10"> 
            <ActivityIndicator IsRunning="{Binding IsBusy}" HorizontalOptions="Center" VerticalOptions="Center" Grid.Column="0" Grid.Row="0"/>
            <CarouselView x:Name="PanelCarousel" Grid.Column="0" Grid.Row="0" IndicatorView="IndicatorView" CurrentItem="{Binding SelectedPanel}">

                <CarouselView.EmptyView>
                    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Padding="20">
                        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Padding="20">
                            <Label HorizontalOptions="Center" Text="Welcome to the DCC Panel Controller!" FontAttributes="Bold" />
                            <Label HorizontalOptions="Center" Text="  " />
                            <Label HorizontalOptions="Center" Text="This page will populate once you have created panels and turnout control. To get started, head over to the instructions page and then to Settings to connect to your WiThrottle server." />
                            <Label HorizontalOptions="Center" Text="  " />
                        </StackLayout>
                        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Padding="20">
                            <Button Style="{StaticResource ButtonOutline}" Text="Instructions" Margin="0,10,0,10" Clicked="ButtonInstructions_OnClicked" />
                            <Button Style="{StaticResource ButtonOutline}" Text="About" Margin="0,10,0,10" Clicked="ButtonAbout_OnClicked" />
                        </StackLayout>
                    </StackLayout>
                </CarouselView.EmptyView>
                
                <CarouselView.ItemsSource>
                    <Binding Path="Panels" />
                </CarouselView.ItemsSource>
                
                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <view:ControlPanelView Panel="{Binding .}"
                                               DesignMode="False"
                                               ShowGrid="False"
                                               BackgroundColor="White"
                                               HorizontalOptions="Fill"
                                               VerticalOptions="Fill"/>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>
            <IndicatorView x:Name="IndicatorView"
                           Grid.Column="0"
                           Grid.Row="1"
                           IndicatorColor="LightGray"
                           SelectedIndicatorColor="DarkGray"
                           HorizontalOptions="Center" />
        </Grid>
        
    </ContentPage.Content>
</ContentPage>
