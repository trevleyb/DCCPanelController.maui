<ContentView
    x:Class="DCCPanelController.View.ControlPanelView"
    x:DataType="view:ControlPanelView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:helpers="clr-namespace:DCCPanelController.View.Helpers"
    xmlns:view="clr-namespace:DCCPanelController.View"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ContentView.Resources>
        <ResourceDictionary />
    </ContentView.Resources>

    <Grid
        HorizontalOptions="Fill"
        VerticalOptions="Fill"
        x:Name="MainGrid">
        <AbsoluteLayout
            HorizontalOptions="Fill"
            VerticalOptions="Fill"
            x:Name="ControlPanelLayout">
            <Grid
                AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1"
                AbsoluteLayout.LayoutFlags="PositionProportional"
                BackgroundColor="{Binding Panel.BackgroundColor, FallbackValue=#FFFFFF}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                x:Name="DynamicGrid" />
        </AbsoluteLayout>
    </Grid>
</ContentView>