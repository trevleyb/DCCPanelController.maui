<?xml version="1.0" encoding="utf-8" ?>

<ContentView
    x:Class="DCCPanelController.View.Actions.TurnoutActionsGrid"
    x:DataType="local:TurnoutActionsGridViewModel"
    x:Name="TurnoutActionsGridView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:behaviors="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:components="clr-namespace:DCCPanelController.View.Components"
    xmlns:dataModel="clr-namespace:DCCPanelController.Models.DataModel"
    xmlns:local="clr-namespace:DCCPanelController.View.Actions"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ContentView.Resources>
        <ResourceDictionary />
    </ContentView.Resources>

    <ContentView.Content>
        <VerticalStackLayout>
            <Grid ColumnDefinitions="120,auto,auto">
                <Label
                    FontAttributes="Bold"
                    Grid.Column="0"
                    HeightRequest="25"
                    Text="Turnout Actions"
                    TextColor="{StaticResource Primary}" />
                <ImageButton
                    Aspect="Center"
                    Command="{Binding AddRowCommand}"
                    Grid.Column="1"
                    HeightRequest="25"
                    IsEnabled="{Binding IsAddButtonEnabled}"
                    Margin="0,-5,0,0"
                    Source="plus_circle.png"
                    WidthRequest="25">
                    <ImageButton.Behaviors>
                        <behaviors:IconTintColorBehavior TintColor="{StaticResource Primary}" />
                    </ImageButton.Behaviors>
                </ImageButton>
                <Label
                    Grid.Column="2"
                    HeightRequest="25"
                    Style="{StaticResource SmallLabel}"
                    Text="{Binding NoDataText}"
                    VerticalOptions="Center" />
            </Grid>

            <Grid HeightRequest="{Binding ControlHeight, Mode=OneWay}" IsVisible="{Binding IsGridVisible}">
                <ListView
                    HorizontalOptions="Fill"
                    ItemsSource="{Binding TurnoutPanelActions}"
                    VerticalOptions="Fill"
                    WidthRequest="440">

                    <ListView.Header>
                        <Grid
                            ColumnDefinitions="3*,4*,4*,3*,1*"
                            ColumnSpacing="2"
                            RowDefinitions="25">
                            <BoxView Background="{StaticResource Primary}" Grid.Column="0" />
                            <BoxView Background="{StaticResource Primary}" Grid.Column="1" />
                            <BoxView Background="{StaticResource Primary}" Grid.Column="2" />
                            <BoxView Background="{StaticResource Primary}" Grid.Column="3" />
                            <BoxView Background="{StaticResource Primary}" Grid.Column="4" />

                            <Label
                                FontAttributes="Bold"
                                Grid.Column="0"
                                Margin="2"
                                Text="Turnout"
                                TextColor="{StaticResource White}" />

                            <components:ButtonStateControl
                                CanToggleState="False"
                                Grid.Column="1"
                                HorizontalOptions="Start"
                                IsVisible="{Binding IsButtonContext}"
                                Margin="2"
                                State="On" />

                            <components:ButtonStateControl
                                CanToggleState="False"
                                Grid.Column="2"
                                HorizontalOptions="Start"
                                IsVisible="{Binding IsButtonContext}"
                                Margin="2"
                                State="Off" />

                            <components:TurnoutStateControl
                                CanToggleState="False"
                                Grid.Column="1"
                                HorizontalOptions="Start"
                                IsVisible="{Binding IsTurnoutContext}"
                                Margin="2"
                                State="Closed" />

                            <components:TurnoutStateControl
                                CanToggleState="False"
                                Grid.Column="2"
                                HorizontalOptions="Start"
                                IsVisible="{Binding IsTurnoutContext}"
                                Margin="2"
                                State="Thrown" />

                            <Label
                                FontAttributes="Bold"
                                Grid.Column="3"
                                HorizontalOptions="Center"
                                Margin="2"
                                Text="Cascade?"
                                TextColor="{StaticResource White}" />

                            <Image
                                Grid.Column="4"
                                Margin="2"
                                Source="trash.png">
                                <Image.Behaviors>
                                    <behaviors:IconTintColorBehavior TintColor="Red" />
                                </Image.Behaviors>
                            </Image>

                        </Grid>
                    </ListView.Header>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell x:DataType="dataModel:TurnoutAction">
                                <Grid
                                    ColumnDefinitions="3*,4*,4*,3*,1*"
                                    ColumnSpacing="2"
                                    RowDefinitions="25"
                                    VerticalOptions="Center">
                                    <Picker
                                        Focused="IDPicker_OnFocused"
                                        Grid.Column="0"
                                        ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type local:TurnoutActionsGridViewModel}}, x:DataType=local:TurnoutActionsGridViewModel, Path=AvailableTurnouts}"
                                        Margin="2,0,0,0"
                                        SelectedItem="{Binding Id}"
                                        TextColor="Black"
                                        Unfocused="IDPicker_OnUnfocused"
                                        VerticalOptions="Center"
                                        x:Name="IDPicker" />

                                    <components:TurnoutStateControl
                                        CanSetStateUnknown="True"
                                        CanToggleState="True"
                                        Grid.Column="1"
                                        HorizontalOptions="Start"
                                        Margin="2,0,0,0"
                                        State="{Binding WhenClosed, Mode=TwoWay}"
                                        UnknownText="Leave-AsIs"/>

                                    <components:TurnoutStateControl
                                        CanSetStateUnknown="True"
                                        CanToggleState="True"
                                        Grid.Column="2"
                                        HorizontalOptions="Start"
                                        Margin="2,0,0,0"
                                        State="{Binding WhenThrown, Mode=TwoWay}"
                                        UnknownText="Leave-AsIs"/>

                                    <CheckBox
                                        Grid.Column="3"
                                        IsChecked="{Binding Cascade}"
                                        Margin="2,0,0,0"
                                        VerticalOptions="Center"
                                        x:DataType="dataModel:TurnoutAction" />

                                    <ImageButton
                                        Aspect="Center"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type local:TurnoutActionsGridViewModel}}, x:DataType=local:TurnoutActionsGridViewModel, Path=RemoveRowCommand}"
                                        CommandParameter="{Binding .}"
                                        Grid.Column="4"
                                        HeightRequest="10"
                                        Margin="2,0,0,0"
                                        Source="minus_circle.png"
                                        VerticalOptions="Center"
                                        WidthRequest="10" />
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </VerticalStackLayout>
    </ContentView.Content>
</ContentView>