<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="{Binding Panel.Name}"
             xmlns:viewModel="clr-namespace:DCCPanelController.ViewModel"
             x:DataType="viewModel:PanelEditorViewModel"
             x:Class="DCCPanelController.View.PanelEditorPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="save.png" />
        <ToolbarItem IconImageSource="trash.png" />
        <ToolbarItem/>
        <ToolbarItem IconImageSource="tuning.png" />
        <ToolbarItem IconImageSource="transpose.png" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
    </ContentPage.Content>
</ContentPage>