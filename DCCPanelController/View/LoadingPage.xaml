<?xml version="1.0" encoding="utf-8"?>

<ContentPage
    x:Class="DCCPanelController.View.LoadingPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:DCCPanelController.View"
    x:DataType="view:LoadingPage"
    ios:Page.UseSafeArea="True"
    BackgroundColor="#FF4F8F00"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls">

    <AbsoluteLayout>
        <!-- Your splash logo sits dead center (unchanged). -->
        <Image x:Name="SplashLogo"
               Source="splash.png"
               Aspect="AspectFit"
               AbsoluteLayout.LayoutFlags="PositionProportional"
               AbsoluteLayout.LayoutBounds="0.5,0.4,-1,-1"/>

        <!-- Version text positioned directly under SplashLogo from code-behind. -->
        <Grid x:Name="VersionGrid"
            AbsoluteLayout.LayoutFlags="PositionProportional"
            AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1">
            
            <VerticalStackLayout>
            <Label
                x:Name="VersionLabel"
                FontSize="12"
                TextColor="White"
                HorizontalTextAlignment="Center"/>

            <Label
                Text="{Binding StatusText}"
                FontSize="14"
                Margin="0,50,0,0"
                LineBreakMode="TailTruncation"
                TextColor="WhiteSmoke"
                HorizontalTextAlignment="Center"/>

            <ProgressBar
                Margin="0,20,0,0"
                Progress="{Binding Progress}"
                IsVisible="{Binding IsBusy}"
                HeightRequest="4"/>
            </VerticalStackLayout>
        </Grid>
    </AbsoluteLayout>
</ContentPage>